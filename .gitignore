# Build artifacts
*.o
*.d
*.dylib
*.so
*.a
machpsdr
MacHPSDR.app/
MacHPSDR.zip

# Debian package staging / generated packages (regenerated, never committed)
pkg/
*.deb

# Vendored WDSP (wdsp/) is now tracked as part of this repo; its own
# wdsp/.gitignore plus the globals above keep its build artifacts out.

# Test/scratch data
iq.wav
*.wav

# Screenshots / images
scr.jpg
screenshot.jpg

# macOS / editor / agent
.DS_Store
.claude/
.vscode/

# transient build artifact (entitlements for codesign)
machpsdr_entitlements.plist

# Internal roadmap (not for distribution)
doc/TODO-parity.md

# Claude Code project guidance (kept local, not distributed)
CLAUDE.md

# stray build-warning capture
build_warn.log

# Build-flag stamp used to force a rebuild when feature flags change
.build-flags
hfdl_offline
hfdl_offline.dSYM/
apt_offline
apt_offline.dSYM/

# apt_offline default output (a decoded pass, regenerated on demand)
apt.png
